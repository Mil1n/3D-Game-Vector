{
  "name": "vector-null",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A fast procedural sci-fi FPS vertical slice with reality-shifting arenas.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test"
  },
  "dependencies": {
    "cannon-es": "^0.20.0",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.1.2"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
